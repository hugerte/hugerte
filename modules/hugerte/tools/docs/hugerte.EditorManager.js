/**
 * This is the base class for the link:../hugerte.root/[`hugerte`] global.
 *
 * This class is a factory for link:../hugerte.editor/[`hugerte.Editor`] instances.
 *
 * And the link:../hugerte.root/[`hugerte`] global is the only
 * EditorManager instance the class is expected to create.
 *
 * @example
 * hugerte.EditorManager.init({});
 *
 * @class hugerte.EditorManager
 * @static
 */
